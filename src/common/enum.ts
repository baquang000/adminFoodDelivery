export enum SCREEN_ENUM {
    PRODUCT = 'product',
    CATEGORY = 'category',
    ORDER = 'order',
    COMMENT = 'comment',
    DASHBOARD = 'dashboard'
}

export enum ACTION_ENUM {
    CREATE = 'create',
    UPDATE = 'update'
}